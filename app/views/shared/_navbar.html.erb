<div class="navbar navbar-expand-sm navbar-light mr-5">
  <div class="navbar-btn"> </div>

  <div class="navbar-title">
    <h3 class="text-dark m-0 p-0"><%= @navbar_title %></h3>
  </div>

  <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"> -->
  <button class="navbar-btn navbar-toggler"  id="navbarDropdown" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
    <% if user_signed_in? %>
      <%= link_to "Home", root_path, class: "dropdown-item" %>
    <% end %>
    <%= link_to "About", info_path, class: "dropdown-item" %>
    <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
    <%#= link_to "Profile", profile_path, class: "dropdown-item" %>
    <% if user_signed_in? %>
      <%= link_to "Edit", edit_user_registration_path, class: "dropdown-item" %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete,  class: "dropdown-item" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
      <%= link_to "Sign up", new_user_registration_path, class: "dropdown-item" %>
    <% end %>
  </div>
</div>

<% if @navbar_title.present? %>
  <hr>
<% end %>
